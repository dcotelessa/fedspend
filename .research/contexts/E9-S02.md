# Context bundle — E9-S02

**Title:** Backend prod config — migrations, env vars, health check  |  **Epic:** E9

## Files
  - EXISTS: backend/src/config/typeorm.config.ts
  - CREATE: backend/src/migrations/.gitkeep
  - CREATE: backend/src/migrations/1700000000000-Init.ts
  - EXISTS: backend/package.json
  - EXISTS: backend/.env.example
  - EXISTS: backend/src/config/typeorm.config.spec.ts

## Acceptance Criteria
  - [ ] Init migration file exists under backend/src/migrations/
  - [ ] postgres branch has migrationsRun:true and synchronize:false
  - [ ] package.json has typeorm migration script
  - [ ] .env.example documents DATABASE_URL, FRONTEND_URL, NODE_ENV
  - [ ] typeorm.config.spec.ts still passes (updated to cover migrationsRun assertion)
  - [ ] backend builds

## Notes
Generate migration via `pnpm typeorm migration:generate src/migrations/Init -d src/config/typeorm-datasource.ts`. Need a datasource file (TypeORM DataSource for CLI). Or hand-write the migration. migrationsRun:true in the postgres config branch (already asserted by E1-S03 spec but stricter here).

