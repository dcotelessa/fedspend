# Context bundle — E9-S03

**Title:** Frontend prod build config — environment.prod, _redirects in assets  |  **Epic:** E9

## Files
  - EXISTS: frontend/angular.json
  - EXISTS: frontend/src/environments/environment.prod.ts
  - EXISTS: frontend/_redirects
  - CREATE: frontend/build-output.spec.ts

## Acceptance Criteria
  - [ ] environment.prod.ts has apiUrl: ''
  - [ ] angular.json includes _redirects in assets
  - [ ] production build emits _redirects in dist
  - [ ] frontend builds
  - [ ] build-output.spec.ts passes (asserts dist/frontend/browser/_redirects exists post-build)

## Notes
Configure angular.json production fileReplacements to swap environment.ts → environment.prod.ts (if not already). Add _redirects to assets array so it's copied verbatim to dist/frontend/browser/.

