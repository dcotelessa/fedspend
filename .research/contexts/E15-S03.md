# Context bundle — E15-S03

**Title:** Agencies: per-award-type breakdown (real Contracts/Grants/Loans/etc instead of Total)  |  **Epic:** E15

## Files
  - EXISTS: backend/src/sync/sync.constants.ts
  - EXISTS: backend/src/sync/usa-spending.service.ts
  - EXISTS: backend/src/sync/usa-spending.service.spec.ts
  - EXISTS: backend/src/sync/sync.service.ts
  - EXISTS: backend/src/sync/sync.service.spec.ts

## Acceptance Criteria
  - [ ] fetchSpendingByAgency iterates over award types (not a single Total fetch)
  - [ ] fetchSpendingByAgency no longer stamps the literal Total awardTypeLabel
  - [ ] award-type to USASpending code map is defined in sync.constants.ts
  - [ ] usa-spending.service.spec.ts passes (table-driven: per-type fetch + label stamping)
  - [ ] sync.service.spec.ts passes
  - [ ] backend builds cleanly
  - [ ] backend full test suite green

## Notes
CRITICAL - VERIFY-FIRST GATE: the USASpending spending_by_geography endpoint silently ignored filters.awarding_agencies in E14-S08 (every per-agency row stored rollup data until E14-S10 caught it). The same risk applies here: filters.award_type may be accepted but ignored, OR the code mapping may be wrong. Before building, the implementer MUST hit the live API with a known award_type code and confirm the total differs from the rollup (e.g. Contracts-only total < all-awards total). If the endpoint silently ignores award_type, HALT and report back - do not ship a fake breakdown. The AWARD_TYPES constant already exists (sync.constants.ts); this story adds the code mapping. Sync cost after deploy: ~20 agencies x 5 years x 5 types = 500 fetches (one-time backfill, several minutes). The Spotlight needs NO frontend change - buildStackedChartFromRecords/buildTableData/computeInsight already group by awardTypeLabel, so real labels flow through automatically. The SpendingRecord @Unique([agencyId,fiscalYear,quarter,awardTypeLabel]) naturally supports multiple rows per agency-year.

