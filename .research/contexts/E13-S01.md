# Context bundle — E13-S01

**Title:** Dashboard chart: Top-N + Other consolidation  |  **Epic:** E13

## Files
  - EXISTS: frontend/src/app/dashboard/dashboard.component.ts
  - EXISTS: frontend/src/app/dashboard/dashboard.component.spec.ts

## Acceptance Criteria
  - [ ] chartLabels includes 'Other' as last element
  - [ ] component has a buildChart method that groups agencies
  - [ ] dashboard.component.spec.ts passes (table-driven: top-N grouping, all-zero, fewer-than-N, click navigation)
  - [ ] frontend builds

## Notes
The dashboard chart currently plots all 111 agencies — 105 are invisible (totalCents=0). Replace with a Top-N + Other consolidation.

ALGORITHM:
1. Sort agencies by totalCents descending
2. Take top 5 (TOP_N = 5) as individual bars
3. Sum remaining agencies' totalCents into one 'Other' bar
4. Set chartLabels = [name1, name2, name3, name4, name5, 'Other']
5. Set chartDatasets = [{ label: 'Obligated (cents)', data: [c1, c2, c3, c4, c5, sumRest] }]
6. Store top agency ids in a separate array (chartAgencyIds) for click resolution
7. If fewer than TOP_N agencies have data, show only those + Other

CLICK HANDLER:
- onChartClick: get clicked index. If index < chartAgencyIds.length, navigate to /agencies/chartAgencyIds[index]. If 'Other' bar clicked, do nothing.

SPEC TEST TABLE:
- 6 agencies with data, TOP_N=3: 3 bars + 'Other' with sum of remaining 3
- All agencies $0: single 'Other' bar at $0, chartAgencyIds is empty
- 2 agencies with data, TOP_N=5: 2 bars + 'Other'
- Click on top bar: navigate to that agency id
- Click on 'Other' bar: no navigation

Do NOT change dashboard.component.html or dashboard.component.scss. The chart template already binds [data] and (chartClick) — just wire the component logic. Keep CurrencyFormatPipe, cards, nav cards untouched.

