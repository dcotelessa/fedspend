# Context bundle — E12-S05

**Title:** Deploy to Render — verify production build + health check  |  **Epic:** E12

## Files
  - EXISTS: render.yaml
  - EXISTS: frontend/src/environments/environment.prod.ts

## Acceptance Criteria
  - [ ] render.yaml exists with backend + frontend services
  - [ ] environment.prod.ts has empty apiUrl (relative paths)
  - [ ] frontend production build succeeds

## Notes
E9 stories created the render.yaml and prod config but were never tested on actual Render infra. This story verifies the config is correct. The render.yaml should define: (1) backend service running nest start -- from dist/backend/src/main.js, (2) static site for frontend dist/. environment.prod.ts apiUrl should be '' (empty — same-origin). DATABASE_URL will be set by Render's Postgres addon.

