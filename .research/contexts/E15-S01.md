# Context bundle — E15-S01

**Title:** Agencies quick wins: YoY unit fix, drop Award Count, FY reconciliation, range-aware table/insight, agency-list spec  |  **Epic:** E15

## Files
  - EXISTS: frontend/src/app/agency-spotlight/agency-spotlight.component.ts
  - EXISTS: frontend/src/app/agency-spotlight/agency-spotlight.component.html
  - EXISTS: frontend/src/app/agency-spotlight/agency-spotlight.component.spec.ts
  - CREATE: frontend/src/app/agency-list/agency-list.component.spec.ts
  - EXISTS: frontend/src/app/api.service.spec.ts
  - EXISTS: backend/src/agencies/agencies.service.ts

## Acceptance Criteria
  - [ ] updateBadge no longer multiplies yoyChange by 100 (consumes the backend percentage directly)
  - [ ] Award Count is absent from the Spotlight displayedColumns
  - [ ] Award Count column block is absent from the Spotlight template
  - [ ] populateAvailableYears reconciles fiscalYearStart/End against derived years
  - [ ] Spotlight table header references fiscalYearStart (range, not just end)
  - [ ] agency-list.component.spec.ts exists and passes (table-driven)
  - [ ] agency-spotlight.component.spec.ts passes (badge rows use percentage inputs)
  - [ ] meta.pageSize is derived from data length, not hardcoded 6
  - [ ] frontend builds cleanly
  - [ ] backend builds cleanly

## Notes
YoY unit mismatch root cause: backend agencies.service.ts:48 computes (current-prior)/prior*100 (a percentage number), frontend agency-spotlight.component.ts:142,145 multiplies by 100 again. The two spec halves encode opposite assumptions (backend spec uses 25/200/-50; frontend spec uses 1.0/-0.5/0.05). Per decision: fix on the FRONTEND (keep backend emitting %), so update the frontend badge spec rows + the api.service.spec mock. Award Count: same class as the Geography E14-S12 fix - the column has no data source (USASpending geography endpoint returns only aggregated_amount/population/per_capita), so transformGeoRows hardcodes awardCount:0; remove the column from the table and stop aggregating it in buildTableData. FY reconciliation: mirror the Geography loadFiscalYears pattern - after populateAvailableYears derives the set, clamp fiscalYearStart/End into [min..max] so the From dropdown always shows a valid selection. Range-aware table/insight: aggregateAwardTypesForYear currently keys off fiscalYearEnd only; rename/extend to aggregate across [start..end] and update the table header to show the range. Add agency-list.component.spec.ts (currently missing - AGENTS.md requires a parallel spec) with table-driven rows for: renders agencies, formats totals via currencyFormat when >0, shows View details otherwise, navigates to /agencies/:id on click. No pi/build-agent run required for this story; it can be built directly with harness=opencode.

