# QA Findings — E5-S02

## Flagged for a follow-up story
- Delta-sign spec deviation: the story requires delta = recipient − performance always, but the impl computes selectedScope − oppositeScope. When scope=performance the sign flips. This is locked into test row 2 (named "negative" but asserting +1_000_000_000), so fixing it is a behavior change + assertion change → belongs in a new story, not QA.
- agencyList getter returns [] once an agency is selected (agencyId() === null ? [...] : []), emptying the dropdown on selection. Unrelated to the delta feature and untested; flagged rather than touched.
