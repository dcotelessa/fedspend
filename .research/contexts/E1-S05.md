# Context bundle — E1-S05

**Title:** Define 5 TypeORM entities with columns, types, relations, and unique constraints  |  **Epic:** E1

## Files
  - CREATE: backend/src/agencies/agency.entity.ts
  - CREATE: backend/src/spending/spending-record.entity.ts
  - CREATE: backend/src/geography/geo-spending-snapshot.entity.ts
  - CREATE: backend/src/disaster/disaster-funding-record.entity.ts
  - CREATE: backend/src/disaster/disaster-recovery-ratio.entity.ts
  - CREATE: backend/src/entities.spec.ts
  - EXISTS: backend/src/agencies/agencies.module.ts
  - EXISTS: backend/src/spending/spending.module.ts
  - EXISTS: backend/src/geography/geography.module.ts
  - EXISTS: backend/src/disaster/disaster.module.ts

## Acceptance Criteria
  - [ ] all 5 entity files exist
  - [ ] monetary columns use integer type (not float)
  - [ ] recoveryRatio is float/decimal (not integer)
  - [ ] SpendingRecord has @Unique on (agencyId, fiscalYear, quarter, awardTypeLabel)
  - [ ] GeoSpendingSnapshot has @Unique on (stateCode, fiscalYear, agencyId, scope)
  - [ ] DisasterFundingRecord has @Unique on (defGroup, stateCode)
  - [ ] entities.spec.ts passes
  - [ ] backend builds

## Notes
Per resolution: perCapita stored as integer cents/person (not dollars). Per resolution: recoveryRatio is dimensionless float — the cents rule does NOT apply. Per resolution: GeoSpendingSnapshot unique constraint includes nullable agencyId; SQLite allows multiple NULL agencyId rows — accept this, dedupe at app level in Epic 2. Use the standard @Unique decorator. Entities use @Column({ type: 'integer' }) for cents fields in sqlite. Use TypeORM metadata in the spec (getMetadataArgsStorage) to assert columns and unique constraints declaratively — do NOT spin up a database for unit tests.

