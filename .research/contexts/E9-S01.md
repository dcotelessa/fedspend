# Context bundle — E9-S01

**Title:** render.yaml + Render config  |  **Epic:** E9

## Files
  - CREATE: render.yaml
  - CREATE: frontend/_redirects
  - CREATE: frontend/public/_redirects
  - CREATE: backend/test/render-yaml.spec.ts

## Acceptance Criteria
  - [ ] render.yaml exists with web service (fedspend-api), static site (fedspend-ui), database (fedspend-db)
  - [ ] backend buildCommand uses pnpm
  - [ ] _redirects file present for SPA routing
  - [ ] yaml is valid
  - [ ] render-yaml.spec.ts passes (parses yaml, asserts 3 services + database)

## Notes
render.yaml spec per epic-09 doc: backend builds via `cd backend && pnpm install --frozen-lockfile && pnpm build`, starts via `cd backend && node dist/main.js`. Frontend builds via `cd frontend && pnpm install --frozen-lockfile && pnpm build`, staticPublishPath `frontend/dist/frontend/browser`. _redirects file is copied to dist via angular.json assets config (covered in E9-S03).

