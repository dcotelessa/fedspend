# Context bundle — E1-S02

**Title:** Configure @shared/* path alias in tsconfig  |  **Epic:** E1

## Files
  - EXISTS: backend/tsconfig.json
  - EXISTS: backend/tsconfig.build.json
  - CREATE: backend/src/shared-alias.spec.ts

## Acceptance Criteria
  - [ ] tsconfig.json maps @shared/* to ../shared/*
  - [ ] tsconfig.build.json does NOT exclude ../shared
  - [ ] shared-alias.spec.ts passes
  - [ ] backend still builds with alias resolved

## Notes
Per resolution: do NOT exclude ../shared from tsconfig.build.json. TypeScript needs to resolve @shared/* types at compile time; they are type-only imports and produce no output in dist/.

