# Context bundle — E14-S05

**Title:** Geography sync dedup — clear NULL-agencyId rollup before bulk-insert  |  **Epic:** E14

## Files
  - EXISTS: backend/src/sync/sync.service.ts
  - EXISTS: backend/src/sync/sync.service.spec.ts

## Acceptance Criteria
  - [ ] IsNull is imported from typeorm in sync.service.ts
  - [ ] syncGeography clears the NULL-agencyId batch via geoRepo.delete (not an upsert loop)
  - [ ] syncGeography no longer calls geoRepo.upsert
  - [ ] syncGeography bulk-inserts via geoRepo.save
  - [ ] sync.service.spec.ts passes (table-driven: clear-then-bulk-insert contract)
  - [ ] backend builds cleanly

## Notes
The NULL-in-unique-constraint gotcha affects Postgres in prod too, not just SQLite — the sync fix is the real repair (deduping the dev DB is a one-time local cleanup since dev.db is gitignored). The clear-then-insert is safe because the geography rollup always has agencyId=NULL and fetchGeoSnapshots returns the full batch each run. Do not reintroduce upsert for the geo rollup; other syncs (agency/spending/disaster/ratio) keep upsert because their conflict-target columns are non-nullable.

