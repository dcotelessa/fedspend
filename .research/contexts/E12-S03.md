# Context bundle — E12-S03

**Title:** Dark mode smoke test + fix any remaining theme gaps  |  **Epic:** E12

## Files
  - EXISTS: frontend/src/styles.scss
  - EXISTS: frontend/src/app/theme.service.ts

## Acceptance Criteria
  - [ ] styles.scss has dark-theme overrides for mat-card, mat-table, and page background
  - [ ] frontend builds
  - [ ] all frontend specs pass

## Notes
Dark mode CSS uses body.dark-theme with !important on background-color. Material M3 theme variables are scoped to .dark-theme. Remaining gaps to check: mat-card content text color, mat-table row hover, chart canvas background (Chart.js transparent — fine), mat-tab-body content, mat-paginator. Add overrides in styles.scss as needed. The ThemeService already toggles class on body + CDK overlay container via effect().

