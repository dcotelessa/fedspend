# Context bundle — E1-S01

**Title:** Scaffold NestJS project, install dependencies, remove defaults  |  **Epic:** E1

## Files
  - CREATE: backend/package.json
  - CREATE: backend/tsconfig.json
  - CREATE: backend/tsconfig.build.json
  - CREATE: backend/nest-cli.json
  - CREATE: backend/.env.example
  - CREATE: backend/src/main.ts
  - CREATE: backend/src/app.module.ts
  - CREATE: backend/src/app.module.spec.ts

## Acceptance Criteria
  - [ ] backend/package.json has all required runtime dependencies
  - [ ] backend/src/main.ts exists
  - [ ] backend/src/app.module.ts exists
  - [ ] default AppController and AppService removed (no files, no registrations)
  - [ ] app.module.spec.ts passes
  - [ ] backend builds cleanly (pnpm build)

## Notes
Use `npx @nestjs/cli new backend --package-manager pnpm --skip-git --strict` then surgically remove AppController/AppService. The CLI also generates app.controller.spec.ts and app.service.spec.ts — remove those too. The spec file app.module.spec.ts must import AppModule and assert it is a class decorated with @Module. Do NOT add TypeORM, ConfigModule, or any feature module here — those belong to later stories.

