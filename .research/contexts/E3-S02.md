# Context bundle — E3-S02

**Title:** Agencies query endpoints (list, summary, spotlight)  |  **Epic:** E3

## Files
  - EXISTS: backend/src/agencies/agencies.service.ts
  - EXISTS: backend/src/agencies/agencies.controller.ts
  - CREATE: backend/src/agencies/agencies.service.spec.ts
  - CREATE: backend/src/agencies/agencies.controller.spec.ts
  - EXISTS: backend/src/agencies/agencies.module.ts

## Acceptance Criteria
  - [ ] 3 endpoints registered in AgenciesController
  - [ ] AgenciesService has findAllWithTotals, findSummary, findSpotlight methods
  - [ ] agencies.service.spec.ts passes (table-driven: totals aggregation, YoY calc, spotlight grouping)
  - [ ] agencies.controller.spec.ts passes (table-driven: each endpoint calls service with correct args, returns ApiResponse<T> for list endpoints)
  - [ ] backend builds

## Notes
List endpoint wraps in ApiResponse<T[]> (paginated). Single-item endpoints return T directly (no wrapper) per E1-S06. YoY change = (current - prior) / prior * 100, guard divide-by-zero (return 0 if prior=0). Spotlight groups by fiscalYear then awardTypeLabel.

