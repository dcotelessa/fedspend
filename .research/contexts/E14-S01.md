# Context bundle — E14-S01

**Title:** Geography: dynamic agency + FY dropdowns, latest-year default, reliable delta insight  |  **Epic:** E14

## Files
  - EXISTS: frontend/src/app/geographic-view/geographic-view.component.ts
  - EXISTS: frontend/src/app/geographic-view/geographic-view.component.html
  - EXISTS: frontend/src/app/geographic-view/geographic-view.component.spec.ts

## Acceptance Criteria
  - [ ] agencyList is no longer a hardcoded static array (fetched from ApiService.getAgencies)
  - [ ] fiscalYearList is derived dynamically (not a hardcoded literal array)
  - [ ] default fiscalYear resolves to the latest year present in the data (2024), not a hardcoded 2020 literal
  - [ ] delta insight prose names the top state and references contractors receive / performance
  - [ ] component no longer nulls this.delta inside processData on the primary load path
  - [ ] geographic-view.component.spec.ts passes (table-driven: agency list populated from mock ApiService, FY list derived from snapshots, default FY = max year, delta positive/negative/zero)
  - [ ] frontend builds cleanly

## Notes
The dev DB currently contains 114 geo rows, all FY2024 / agencyId=null / scope=recipient; the page renders blank today because fiscalYear defaults to 2020. Resolve by deriving fiscalYearList client-side from distinct fiscalYear values seen in the loaded snapshots and defaulting to Math.max of those years. Agencies come from ApiService.getAgencies(); cache them in a field populated on first load (do not refetch on every control change). Delta prose target: "Top state <State> contractors receive $X while only $Y is performed in-state (delta $Z)." Do NOT add a new backend endpoint for years (resolution: client-side derivation). Do NOT add a spec for the HTML string beyond the grep check.

