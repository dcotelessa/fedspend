# Context bundle — E1-S03

**Title:** TypeORM dual-database config (SQLite/Postgres) + AppModule wiring  |  **Epic:** E1

## Files
  - CREATE: backend/src/config/typeorm.config.ts
  - CREATE: backend/src/config/typeorm.config.spec.ts
  - EXISTS: backend/src/app.module.ts

## Acceptance Criteria
  - [ ] backend/src/config/typeorm.config.ts exists
  - [ ] config factory calls mkdirSync('data', {recursive:true}) for sqlite path
  - [ ] entity glob pattern is __dirname + '/../**/*.entity{.ts,.js}'
  - [ ] typeorm.config.spec.ts passes (table-driven: DATABASE_URL set → postgres, unset → sqlite)
  - [ ] AppModule imports ConfigModule and TypeOrmModule.forRootAsync
  - [ ] backend builds

## Notes
Per resolution: the glob __dirname + '/../**/*.entity{.ts,.js}' is correct because at runtime typeorm.config.ts compiles to dist/config/, so __dirname = dist/config/, ../ = dist/, glob matches dist/**/*.entity.js. Per resolution: mkdirSync('data', {recursive:true}) MUST be called in the factory before returning the sqlite config — better-sqlite3 won't create the parent dir. The spec must mock ConfigService.get('DATABASE_URL') and assert both branches. Do NOT use synchronize:true in the postgres branch.

