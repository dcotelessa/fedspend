# Context bundle — E2-S04

**Title:** Recovery ratio calculator — pure module with edge cases  |  **Epic:** E2

## Files
  - CREATE: backend/src/sync/recovery-ratio.ts
  - CREATE: backend/src/sync/recovery-ratio.spec.ts

## Acceptance Criteria
  - [ ] recovery-ratio.ts exports computeRecoveryRatio
  - [ ] recovery-ratio.spec.ts passes (table-driven across 4 edge cases + boundaries)
  - [ ] function is pure (no service injection, no side effects)
  - [ ] backend builds

## Notes
Edge cases per epic-02 spec: both 0 → 1.0 (neutral); fema 0 fed >0 → Infinity (well-resourced); fema >0 fed 0 → 0 (complete gap); both >0 → fed/fema. Spec testTable must include boundary values like {fema:1, fed:Number.MAX_SAFE_INTEGER} to verify no overflow. This is a pure helper — not @Injectable — because it has no dependencies. SyncService in E2-S05 imports and calls it.

