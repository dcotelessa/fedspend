# Context bundle — E14-S03

**Title:** Disaster: fix FY mat-select binding + move defGroup overview filtering server-side  |  **Epic:** E14

## Files
  - EXISTS: frontend/src/app/disaster-lens/disaster-lens.component.html
  - EXISTS: frontend/src/app/disaster-lens/disaster-lens.component.ts
  - EXISTS: frontend/src/app/disaster-lens/disaster-lens.component.spec.ts
  - EXISTS: backend/src/disaster/disaster.service.ts
  - EXISTS: backend/src/disaster/disaster.service.spec.ts
  - EXISTS: backend/src/disaster/disaster.controller.ts

## Acceptance Criteria
  - [ ] No invalid [(value)] binding remains on the disaster FY mat-select
  - [ ] DisasterService.getOverview filters by defGroup server-side
  - [ ] Disaster controller forwards defGroup query param
  - [ ] Frontend passes defGroup to getDisasterOverview instead of filtering overview client-side by tab
  - [ ] disaster-lens.component.spec.ts passes
  - [ ] disaster.service.spec.ts passes (table-driven: getOverview with defGroup returns only matching rows, without defGroup returns all)
  - [ ] frontend and backend build cleanly

## Notes
ApiService.getDisasterOverview currently takes no params — extend its signature to accept { defGroup?: string } and forward via toParams, mirroring getDisasterStates. Do not change the overview response shape. The client-side fallback-to-0 behavior is the bug being fixed: every tab must now query the server for its own overview row. If a defGroup genuinely has no data the server returns [] and the cards legitimately show 0 — that is correct, not a regression.

