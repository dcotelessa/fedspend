# Context bundle — E2-S06

**Title:** SyncController — POST /sync/* endpoints  |  **Epic:** E2

## Files
  - CREATE: backend/src/sync/sync.controller.ts
  - CREATE: backend/src/sync/sync.controller.spec.ts
  - EXISTS: backend/src/sync/sync.module.ts

## Acceptance Criteria
  - [ ] sync.controller.ts exists with all 5 endpoints
  - [ ] SyncController registered in SyncModule controllers
  - [ ] sync.controller.spec.ts passes (table-driven: each endpoint calls the right service method, returns expected wrapper shape)
  - [ ] backend builds

## Notes
Thin controller: each method is 2-3 lines — parse DTO (empty for triggers), call one service method, return result. Use @Sse() or just @Post() per endpoint. GET /sync/status returns the in-memory timestamp map from SyncService. Spec mocks SyncService and asserts each endpoint calls the expected method with no args (for full sync) and returns the result. Use @Controller('sync') prefix.

