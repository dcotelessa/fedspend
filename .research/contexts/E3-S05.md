# Context bundle — E3-S05

**Title:** E2E query API tests with seeded data  |  **Epic:** E3

## Files
  - CREATE: backend/test/query.e2e-spec.ts
  - CREATE: backend/test/seed.ts

## Acceptance Criteria
  - [ ] all backend specs pass
  - [ ] query e2e spec passes (every endpoint exercised with seed data)
  - [ ] backend builds

## Notes
seed.ts inserts 6 agencies + 5 sample SpendingRecords + 5 GeoSpendingSnapshots + 5 DisasterFundingRecords + 5 DisasterRecoveryRatios into an in-memory sqlite. E2E spec uses Test.createTestingModule + supertest to hit every endpoint and assert response shape + filtering correctness. Do NOT test validation rejection here (covered in DTO specs).

