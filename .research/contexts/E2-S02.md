# Context bundle — E2-S02

**Title:** UsaSpendingService — Fetch/Transform/Return deep module  |  **Epic:** E2

## Files
  - CREATE: backend/src/sync/usa-spending.service.ts
  - CREATE: backend/src/sync/usa-spending.service.spec.ts
  - EXISTS: backend/src/sync/sync.module.ts

## Acceptance Criteria
  - [ ] usa-spending.service.ts exists with all 4 fetch methods
  - [ ] service transforms dollar amounts to integer cents
  - [ ] retry logic present (3 attempts, exponential backoff)
  - [ ] UsaSpendingService registered in SyncModule providers
  - [ ] usa-spending.service.spec.ts passes (table-driven: transform correctness per award type, transform correctness per geo scope, retry-then-success, retry-then-failure)
  - [ ] backend builds

## Notes
Deep module per AGENTS.md Skill 3: ONE file, top-to-bottom legible. The Fetch (HTTP), Transform (raw dollars → cents), and Return (typed) steps stay together. Use global fetch() — no axios dependency. Mock fetch in the spec via jest.spyOn(global, 'fetch'). The spec testTable must cover: (a) raw dollar field 1234.56 → obligatedAmount 123456 cents, (b) pagination handled across multiple pages, (c) HTTP 500 then 200 → retry succeeds, (d) HTTP 500 thrice → throws after 3 attempts. Do NOT call the real USASpending API in unit tests — all fetches mocked.

