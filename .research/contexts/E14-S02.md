# Context bundle — E14-S02

**Title:** Agency Spotlight: true stacked bar chart via BarChartComponent stacked input + title wiring  |  **Epic:** E14

## Files
  - EXISTS: frontend/src/app/bar-chart/bar-chart.component.ts
  - EXISTS: frontend/src/app/bar-chart/bar-chart.component.spec.ts
  - EXISTS: frontend/src/app/agency-spotlight/agency-spotlight.component.html
  - EXISTS: frontend/src/app/agency-spotlight/agency-spotlight.component.spec.ts

## Acceptance Criteria
  - [ ] BarChartComponent exposes a stacked input
  - [ ] BarChartComponent sets scales stacked when stacked is true
  - [ ] BarChartComponent wires title into chart options plugins.title
  - [ ] Agency Spotlight template passes [stacked]="true" to app-bar-chart
  - [ ] bar-chart.component.spec.ts passes (table-driven: stacked=false options omit scales.stacked, stacked=true options include x.stacked and y.stacked, title present in plugins.title when provided)
  - [ ] agency-spotlight.component.spec.ts passes
  - [ ] frontend builds cleanly

## Notes
Behavior-preserving for Geography and Disaster which also use app-bar-chart with the default (stacked=false) — stacked must default to false so existing callers render exactly as before. Do not change the default option shape; only add the two optional inputs. Use Angular signal inputs if the component already uses signals, otherwise @Input() to match existing style in that file.

