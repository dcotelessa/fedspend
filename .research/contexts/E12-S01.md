# Context bundle — E12-S01

**Title:** Fix N+1 query in AgenciesService.findAllWithTotals  |  **Epic:** E12

## Files
  - EXISTS: backend/src/agencies/agencies.service.ts
  - EXISTS: backend/src/agencies/agencies.service.spec.ts

## Acceptance Criteria
  - [ ] findAllWithTotals uses a single query (no per-agency loop)
  - [ ] agencies.service.spec.ts passes
  - [ ] backend builds

## Notes
Current code: agencies.map(a => spendingRepo.find({ where: { agencyId: a.id } })) — 111 queries. Replace with a single LEFT JOIN agency -> spending_record GROUP BY agency.id computing SUM(obligatedAmount). Use TypeORM QueryBuilder: agencyRepo.createQueryBuilder('agency').leftJoin('agency.spendingRecords', 'sr').select(['agency.id', 'agency.name', 'COALESCE(SUM(sr.obligatedAmount), 0) AS totalCents']).groupBy('agency.id').getRawMany(). Filter by current fiscal year in the JOIN condition.

