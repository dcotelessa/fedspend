# Context bundle — E2-S07

**Title:** Nightly schedule via @nestjs/schedule (2am UTC cron)  |  **Epic:** E2

## Files
  - EXISTS: backend/src/app.module.ts
  - EXISTS: backend/src/sync/sync.service.ts
  - CREATE: backend/src/sync/schedule.spec.ts

## Acceptance Criteria
  - [ ] ScheduleModule.forRoot() registered in AppModule
  - [ ] syncAll has @Cron('0 2 * * *') decorator
  - [ ] schedule.spec.ts passes
  - [ ] backend builds

## Notes
Do NOT actually fire the cron in unit tests — that requires real time. The spec asserts presence of the @Cron decorator on syncAll via reflect-metadata, and that ScheduleModule is in AppModule imports. To verify the cron fires at the right time, you'd need an integration test that's out of scope for Epic 2. Just register it correctly.

