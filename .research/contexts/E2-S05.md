# Context bundle — E2-S05

**Title:** SyncService — orchestration, upserts, retry coordination  |  **Epic:** E2

## Files
  - EXISTS: backend/src/sync/sync.service.ts
  - CREATE: backend/src/sync/sync.service.spec.ts
  - EXISTS: backend/src/sync/sync.module.ts

## Acceptance Criteria
  - [ ] sync.service.ts exposes syncAll, syncAgenciesAndSpending, syncGeography, syncDisaster
  - [ ] SyncService uses upsert pattern (insert-or-update) for all 5 entity types
  - [ ] SyncService imports computeRecoveryRatio from ./recovery-ratio
  - [ ] SyncService registered in SyncModule providers
  - [ ] sync.service.spec.ts passes (table-driven: agencies upsert path, spending upsert path, geo upsert path, disaster upsert path, recovery ratio stored correctly)
  - [ ] backend builds

## Notes
SyncService is the deep module that ties Epic 2 together. Inject UsaSpendingService, OpenFemaService, and 5 repositories (Agency, SpendingRecord, GeoSpendingSnapshot, DisasterFundingRecord, DisasterRecoveryRatio). Upsert = findOne by unique key + save (or repository.upsert if TypeORM supports the unique constraint natively). Mock all 5 repositories AND both upstream services in the spec — the spec must NOT hit any real DB or HTTP. The testTable should cover each of the 4 sync methods individually plus the orchestration of syncAll.

