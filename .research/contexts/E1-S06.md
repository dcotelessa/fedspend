# Context bundle — E1-S06

**Title:** Response wrapper interceptor + @Paginated() decorator  |  **Epic:** E1

## Files
  - CREATE: backend/src/common/response-wrapper.interceptor.ts
  - CREATE: backend/src/common/paginated.decorator.ts
  - CREATE: backend/src/common/response-wrapper.interceptor.spec.ts

## Acceptance Criteria
  - [ ] response-wrapper.interceptor.ts exists
  - [ ] paginated.decorator.ts exists
  - [ ] ApiResponse imported from @shared/interfaces (not duplicated locally)
  - [ ] interceptor spec passes (table-driven: list wraps, single item passes through, non-paginated route unaffected)
  - [ ] backend builds

## Notes
Per resolution: do NOT create common/api-response.interface.ts. ApiResponse<T> already lives in @shared/interfaces/index.ts — import it from there. The interceptor checks for the @Paginated() metadata via reflect-metadata; if present and the response is an array, wrap as {data, meta:{total:arr.length, page, pageSize}}. If absent or response is not an array, pass through unchanged. The spec must mock ExecutionContext and the next handler, run the interceptor with a table of {input shape, hasPaginated, expected output shape}.

