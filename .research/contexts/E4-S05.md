# Context bundle — E4-S05

**Title:** BarChartComponent (shared, ng2-charts wrapper)  |  **Epic:** E4

## Files
  - CREATE: frontend/src/app/bar-chart/bar-chart.component.ts
  - CREATE: frontend/src/app/bar-chart/bar-chart.component.html
  - CREATE: frontend/src/app/bar-chart/bar-chart.component.spec.ts

## Acceptance Criteria
  - [ ] BarChartComponent accepts @Input labels, datasets, title, horizontal
  - [ ] component wraps ng2-charts base-chart
  - [ ] bar-chart.component.spec.ts passes (table-driven: input shapes → expected chart config)
  - [ ] frontend builds

## Notes
Pure presentation component. Spec asserts the computed chart config (type, indexAxis, data) from a table of input shapes. Set indexAxis='y' when horizontal=true. Do NOT try to render canvas in jsdom — assert the computed config instead.

