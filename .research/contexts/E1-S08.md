# Context bundle — E1-S08

**Title:** End-to-end verification of Epic 1  |  **Epic:** E1

## Files
  - CREATE: backend/test/e2e.spec.ts

## Acceptance Criteria
  - [ ] all backend unit specs pass (pnpm test)
  - [ ] backend builds cleanly (pnpm build)
  - [ ] no AppController or AppService anywhere in backend/src
  - [ ] e2e spec passes (boots app, hits /health, checks 5 entity tables in sqlite)

## Notes
The e2e spec uses Test.createTestingModule({ imports: [AppModule] }).compile() then app.getHttpServer() with supertest to GET /health. Then it queries the sqlite file (or DataSource.query) to assert the 5 entity tables exist. If the e2e surfaces a wiring gap (missing module registration, broken import path), fix it inside the files already in scope of prior stories — do NOT create new files. This story's scope is intentionally just the e2e spec; the model may edit previously-committed files only to close wiring gaps surfaced by the e2e.

