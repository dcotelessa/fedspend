# Context bundle — E10-S04

**Title:** Polish tests — cross-layer unit coverage + final build pass  |  **Epic:** E10

## Files
  - EXISTS: backend/src/sync/sync.service.spec.ts
  - EXISTS: frontend/src/app/disaster-lens/disaster-lens.component.spec.ts

## Acceptance Criteria
  - [ ] all backend specs pass (pnpm test)
  - [ ] all frontend specs pass (pnpm test)
  - [ ] backend builds cleanly
  - [ ] frontend builds cleanly
  - [ ] DisasterLens spec asserts table sorts by recoveryRatio ascending by default

## Notes
If the existing specs already cover the 3 polish tests, this story just confirms full-suite green. If DEF grouping or sort-default has gaps, this story closes them by editing the relevant specs (only the spec files in scope).

